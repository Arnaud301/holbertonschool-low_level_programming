#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int letters;

	for (letters > a; letters <= z; letters++)
	{
		printf(letters);
		if (letters == q || letters == e)
		{
			continue();
		}
	}
	putchar('\n');
}
