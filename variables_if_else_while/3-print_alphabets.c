#include <stdio.h>
/**
 *main - entry point
 *Return: 0 on sucess
 */
int main(void)
{
	int a;
	int A;

	for (a = 97; a < 123; a++)
		putchar(a);
	for (A = 65; a < 90; A++)
		putchar(A);
	putchar('\n');
	return (0);
}
